source "https://rubygems.org"

ruby "3.4.8"

# Core Jekyll engine
gem "jekyll", "~> 4.3"

# Theme (can change to any other Jekyll theme)
gem "just-the-docs"

# Plugins (Netlify-compatible)
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
  gem "jekyll-remote-theme", "~> 0.4.3"
end

